# Tests Module (`tests/`)

## Intended Role
Contains automated unit and integration tests using `pytest`.

## Components
- `test_data_loader.py`: Verifies HotpotQA dataset downloading, schema validation, and 200-example sample generation.
