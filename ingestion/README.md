# Ingestion & Parsing Module (`ingestion/`)

## Intended Role
This module will handle document parsing, semantic chunking, entity extraction, and relationship extraction for building the knowledge graph and vector representations.

## Key Responsibilities (DA2 Pipeline)
- Document ingestion & text cleaning.
- Chunking strategies (fixed size, semantic, dynamic).
- Extracting entities, types, and relations for Neo4j graph construction.
